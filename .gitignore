.env
.env.*
node_modules/
dist/
*.log
.claude/

# Firebase service-account credentials — never commit
*firebase-adminsdk*.json
backend/secrets/

# Helper file with single-line FCM key — local copy only
_FCM_PRIVATE_KEY_for_render.txt
